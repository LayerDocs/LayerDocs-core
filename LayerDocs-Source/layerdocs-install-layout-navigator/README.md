# install-layout-navigator

This module provides an abstraction layer over the LayerDocs installation layout's `lib` directory,
generated by the root-level `build.gradle.kts`'s `installDist` and `assembleDevLib` tasks.

This directory contains resources used by the LayerDocs runtime, such as themes, fonts and JavaScript libraries.